"""def largest_num(numbers):
    largest=numbers[0]
    for number in numbers:
        if number>largest:
            largest=number
    return(largest)

numbers=[1,100,300,4]
b=largest_num(numbers)
print("largest number of given list is:",b)
"""

numbers=[1,100,300,4]
largest=numbers[0]
for number in numbers:
        if number>largest:
            largest=number
print("the largest number of given list is:",largest)
      
     















